#include<stdio.h>
#include<string.h>

#include<stdlib.h>
int main() {
    int t,i,j,k,sum=0,l;
    scanf("%d",&t);
    char arr[10000];
    for(;t>0;t--) {
        scanf("%s",arr);
        sum=0;
        l=strlen(arr);
        
    	printf("%d",l);
    }
    return 0;
}
